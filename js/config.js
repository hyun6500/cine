/* =====================================================================
   config.js — 개인 설정 (이 파일만 본인 값으로 채우면 됩니다)
   ★ 앞으로 코드를 업데이트할 때 이 파일은 그대로 두세요.
     다른 js 파일을 새로 덮어써도 키가 지워지지 않습니다.

   ⚠️ 값은 반드시 따옴표 사이에 "끝까지" 붙여넣으세요.
     주소창에 …로 줄여 보이는 걸 복사하면 문자열이 안 닫혀서 앱 전체가 멈춥니다.
     Apps Script URL은 [배포 → 배포 관리]의 복사 아이콘으로 받고, /exec 로 끝나야 정상입니다.
   ===================================================================== */

window.CINE_CONFIG = {
  SHEET_ID: "1xf26fIHRUzwIv2brREBzJZTg_mRwCoXzQbP4WDYTl-4",
  SHEET_NAME: "시청기록",

  TMDB_KEY: "cf02e99c2d012e292753b5fb79fcc5dc",

  /* ↓ 배포 관리에서 복사한 전체 URL로 교체 (…로 끝나면 안 됩니다) */
  APPS_SCRIPT_URL: "",

  /* ★ 비공개 모드 (v2.18)
     true  = 시트를 Apps Script를 거쳐 읽습니다.
             '동반/메모'는 토큰이 없으면 서버에서 지워서 내려보내므로,
             개발자도구를 열어도 브라우저에 아예 도착하지 않습니다.
             ※ 이 모드를 켜기 전에 반드시 시트를 비공개로 돌리세요.
               (공개 상태면 시트 주소를 아는 사람이 그냥 열어볼 수 있어 의미가 없습니다)
             ※ APPS_SCRIPT_URL이 비어 있으면 자동으로 false처럼 동작합니다.
     false = 예전처럼 공개 CSV(gviz)로 직접 읽습니다. 빠르지만 가림막 수준의 보호만 됩니다. */
  PRIVATE_MODE: true,
};

/* ★ 편집 비밀번호(APP_TOKEN)는 더 이상 여기 두지 않습니다.
   Apps Script의 TOKEN 값이 곧 비밀번호이고, 앱에서 직접 입력합니다.
   덕분에 이 파일을 누가 열어봐도 시트에 쓰기를 할 수 없습니다. */
